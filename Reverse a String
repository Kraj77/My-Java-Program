class demo{
    public static void main(String []args){
        String s = "Krishna";
        char c;
        for(int i=s.length()-1;i>=0;i--){
            c=s.charAt(i);
            System.out.print(c);
        }
    }
}
